/**
 * Job openings (CONTENT_ARCHITECTURE §1) — validated by getOpenings() against openingSchema.
 * Empty at launch; the careers page renders its designed empty state (Phase 8).
 */
export const openings: unknown[] = [];
