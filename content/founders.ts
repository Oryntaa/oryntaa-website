/**
 * Founders (CONTENT_ARCHITECTURE §1) — array of four, validated by getFounders() against
 * founderSchema. Empty until Phase 3.3 (names/roles real; photos/bios TODO(content)).
 */
export const founders: unknown[] = [];
