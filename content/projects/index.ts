/**
 * Project registry (CONTENT_ARCHITECTURE §1). Each project's meta is imported here and validated
 * by getProjects() against projectMetaSchema; case-study bodies live beside it as MDX. Empty until
 * Phase 3.3 seeds the six projects with their work-map status/permission values.
 */
export const projects: unknown[] = [];
